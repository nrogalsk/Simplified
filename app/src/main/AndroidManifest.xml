<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.niki.simplified">

    //Give the app permission to access the internet
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/aoo_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/aoo_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EmailActivity"
            android:label="@string/about_email_activity" />
        <activity
            android:name=".ClockActivity"
            android:label="@string/about_clock_activity" />
        <activity
            android:name=".AboutActivity"
            android:label="@string/about_about_activity" />

        <receiver android:name=".AlarmReceiver"></receiver>

        <activity android:name=".WeatherActivity"></activity>
    </application>

</manifest>